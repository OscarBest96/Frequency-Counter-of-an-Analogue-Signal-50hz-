FOLDERS:

All_code_tests:
Contains all the Tests created for all code written including both FPGA and MCU (TESTS ONLY)

Frequency_counter:
contains the quartus and keil projects for the main system. This includes only the most recent versions 
of code.
There is a tests folder within the quartus project. These tests are the most recent testbenches for
the most recent versions of components. 